document.getElementById("photo-close").onclick=function(){let player;$(".photo-player").get("0").style.display="none";var photoPlayer=document.getElementById("photo-player");photoPlayer.style.left="0px",photoPlayer.style.top="0px",photoPlayer.style.width=photoWidth+"px",photoPlayer.style.height=photoHeight+"px"};const rem=16;let radio,photoWidth,photoHeight;autoSize=function(e){let imageTag=$(e.target).parent().get("0"),naturalWidth,naturalHeight,rate=$(e.target).get("0").naturalWidth/$(e.target).get("0").naturalHeight;if("photo-frame"!==imageTag.classList[0]){let normalSize=112,other=normalSize*rate;imageTag.style.width=other+"px",imageTag.style.height=normalSize+"px"}else{let container,maxHeight=.95*$(imageTag).parent().parent().get("0").getBoundingClientRect().height,width=imageTag.getBoundingClientRect().width,height=imageTag.getBoundingClientRect().height,calcWidth=width,calcHeight=height,tempHeight=width/rate;tempHeight<=maxHeight?calcHeight=tempHeight:calcWidth=height*rate,imageTag.style.width=calcWidth+"px",imageTag.style.height=calcHeight+"px",radio=1,photoWidth=calcWidth,photoHeight=calcHeight}},reSetSize=function(){$("#photo-player").width(photoWidth*radio),$("#photo-player").height(photoHeight*radio)};